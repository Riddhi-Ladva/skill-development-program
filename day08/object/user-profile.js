const profile = {
  name: "Riddhi",
  experience: 1.5,
  skills: ["JS", "React", "Node"],
  isOpenToWork: true
};

if (profile.isOpenToWork) {
  console.log(profile.name + " is available for job");
}
